version: "2"
sql:
  - schema: "../schema.sql"
    queries: "../query.sql"
    engine: "postgresql"
    gen:
      gleam:
        package: "db"
        out: "/tmp/sqlode_resolve_paths_test"
        runtime: "raw"
