version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      gleam:
        out: "../../test_output/db"
        runtime: "raw"
        strict_views: true
