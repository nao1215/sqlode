version: "2"
database:
  uri: "postgresql://localhost:5432/mydb"
analyzer:
  plugin: "py"
sql:
  - schema: "test/fixtures/schema.sql"
    queries: "test/fixtures/query.sql"
    engine: "postgresql"
    gen:
      gleam:
        out: "test_output/db"
