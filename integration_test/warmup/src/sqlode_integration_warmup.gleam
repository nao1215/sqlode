//// Placeholder module so this warmup project is a well-formed Gleam
//// package. The module is never executed; the project only exists to
//// carry the pinned `manifest.toml` used by `spec/compile_spec.sh`.

pub fn main() -> Nil {
  Nil
}
